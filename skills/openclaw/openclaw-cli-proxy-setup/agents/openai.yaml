interface:
  display_name: "OpenClaw CLI Proxy Setup"
  short_description: "Install CLIProxyAPI as OpenClaw's local multi-API proxy."
  default_prompt: "Use $openclaw-cli-proxy-setup to install CLIProxyAPI, wire it to OpenClaw as a local multi-API proxy, and verify the setup on this Mac."
